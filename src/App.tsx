import React from "react";
import SearchBar from "./components/SearchBar";

const App = () => {
	return (
		<section className='flex justify-center w-full h-full items-center min-h-80'>
			<SearchBar />
		</section>
	);
};

export default App;
